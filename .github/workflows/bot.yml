name: Run Polybot

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Restore DB
        uses: actions/download-artifact@v4
        with:
          name: polybot-db
        continue-on-error: true

      - name: Restore bankroll
        uses: actions/download-artifact@v4
        with:
          name: bankroll
        continue-on-error: true

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run bot
<<<<<< fix/runtime-issues-17074695525775905240
=======
        run: python main.py
>>>>>> main
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
<<<<<< fix/runtime-issues-17074695525775905240
        run: python main.py
=======
>>>>>> main

      - name: Save DB
        uses: actions/upload-artifact@v4
        with:
          name: polybot-db
          path: polybot.db

      - name: Save bankroll
        uses: actions/upload-artifact@v4
        with:
          name: bankroll
<<<<<< fix/runtime-issues-17074695525775905240
          path: bankroll.txt
=======
          path: bankroll.txt
>>>>>> main
